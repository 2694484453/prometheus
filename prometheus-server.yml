version: "3"
services:
  prometheus:
    image: prom/prometheus:latest #可修改其他版本
    container_name: prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - ./config:/etc/prometheus/config
      - ./rules:/etc/prometheus/rules